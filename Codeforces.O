#include <stdio.h>
int main()
{
    int a, b, c;
    char s;
 
    scanf("%d%c%d", &a, &s, &b);    
    if(s == 43)
        c = a + b;
    
    if(s == 45)
        c = a - b;
    
    if(s == 42)
        c = a * b;
    
    if(s == 47)
        c = a / b;
    
    printf("%d", c);
    return 0;
}
