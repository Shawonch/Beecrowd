#include<stdio.h>
int main()
{
    char ch;
    int n,i,j,k;
    scanf("%c%d",&ch,&n);
    for(i=1;i<=n;i++)
    {
        scanf("%d",&j);
        for(k=1;k<=j;k++)
        {
            printf("%c",ch);
        }
        printf("\n");
    }
 
 
}
