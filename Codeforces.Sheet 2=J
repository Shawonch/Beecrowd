#include<stdio.h>
int main()
{
    int i,j,num;
    scanf("%d",&num);
    for(i=2;i<=num;i++)
    {
        int f=0;
        for(j=2;j<i;j++)
        {
            if(i%j==0)
            {
                f=1;
                break;
            }
        }
        if(f==0)
            printf("%d ",i);
    }
}
