#include<stdio.h>
int main()
{
    int i=1,n=100,max=-1,pos=-1;
    while(i<=n)
    {
        scanf("%d",&n);
        if(n>max)
        {
            max=n;
            pos=i;
        }
        i++;

    }
    printf("%d\n",max);
    printf("%d\n",pos);

    return 0;
}
